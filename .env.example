# Non-secret overrides. The setup screen stores credentials in macOS Keychain.
PORT=8080
REDDIT_REDIRECT_URI=http://localhost:8080/oauth/callback

# Optional environment-only configuration for non-macOS development.
# Never commit real values.
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USERNAME=
REDDIT_REFRESH_TOKEN=
